# LOL 智能分组

一个用于 LOL（英雄联盟）开黑的“分组 + 抽英雄”工具：先把 10 人公平分成蓝红两队，
再按照每个人分到的位置，从 OP.GG 的英雄榜快照里自动抽 1~3 个英雄（默认每人 2 个）。

在线演示：[youngzhou6.space](https://youngzhou6.space)

## ✨ 功能特性

### 分组

- 🎯 **智能平衡**：根据玩家 ELO 档次自动平衡两队实力，让双方总分尽量接近
- 🔀 **真随机**：完全随机分组，位置随机、队伍随机，看谁运气好
- 🔒 **位置锁定**：每个玩家可同时锁定多个位置，系统只在锁定位置内分配，避免位置跑偏
- 📊 **实时对比**：分组后显示双方 ELO 总分、分差与实力评级

### 抽英雄

- 🎮 **按位置自动抽英雄**：分组结果确定后，每位玩家从自己分到的位置池中随机抽英雄，
  数量可在 1~3 之间滑动调整（默认每人 2 个）
- 🗂️ **三种英雄池**：
  - `全英雄`：该位置 OP.GG 榜单全部英雄
  - `热门前 20`：OP.GG 榜单前 20 名
  - `Tier 1~2`：OP.GG 版本强势英雄（Tier 1~2）
- 🚫 **全场英雄不重复**：同一局不会出现重复英雄，也不会有人拿到重复的英雄
- ⚖️ **跨位置公平去重**：同一个英雄如果同时出现在多个位置池（如剑魔可上单/打野），
  被某个位置抽到后，其他位置池会同步排除，不会出现两边都抽到剑魔的情况
- 🎲 **重新抽英雄**：点击后只重抽英雄，不改变队伍和位置
- 👁️ **英雄池预览**：点击顶部「英雄池」可查看当前池子每个位置包含的英雄（头像 + 中文名）

### 数据与体验

- 📦 **OP.GG 版本快照**：英雄数据在构建时写入项目，页面会标注
  “Patch 版本 / 分段 / 数据更新时间”，版本更新后跑一条命令即可刷新
- 🧑‍🤝‍🧑 **自定义玩家**：支持临时添加玩家并逐个位置设置档次
- 🎨 **精美界面**：动态光晕背景 + Framer Motion 动画，移动端响应式适配

## 🎮 使用流程

1. **选择 10 位玩家**：点击玩家标签，已选人数显示在左上角（10/10 时才能开始分组）
2. **锁定位置（可选）**：在每个玩家的位置卡片里点击位置按钮，可同时锁定多个位置
3. **选择分组模式**：
   - `智能平衡`：按 ELO 自动均衡双方实力（默认阈值 15%）
   - `真随机`：只保证能组成 5v5，不做实力均衡
4. **设置抽英雄规则（可选）**：
   - 选择英雄池：`全英雄 / 热门前 20 / Tier 1~2`，默认全英雄
   - 拖动「每人英雄数」滑条：可设为 1~3 个，默认 2 个
5. **点击「⚔️ 开始分组」**：系统同时完成“分组 + 抽英雄”
6. **查看与重抽**：每人名下显示对应数量的英雄卡片（头像 + 中文名 + 胜率/登场率，
   悬停可看 OP.GG 排名）；点「🎲 重新抽英雄」只重抽英雄，队伍和位置保持不变
7. **切换规则**：分组后修改英雄池或每人数量会立即按新规则重抽；
   点击「英雄池」下的「👁 查看英雄」可预览每个位置的可选英雄

## 🧠 算法原理

### 一、分组算法

#### 目标

把 10 人分成蓝红两队（每队 5 人、五个位置各一人），在满足位置约束的前提下，
让两队的 ELO 总分差尽量小。

#### 分配流程

1. 打乱玩家顺序（每次尝试都重新打乱）
2. 依次给每位玩家分配一个位置
3. 每次分配都枚举该玩家可去的位置，选择“让双方 ELO 分差更小”的方案
4. 有位置锁定时，只从锁定的位置中选择；打乱顺序后多次尝试，能找到更优组合

#### 两种模式

- `智能平衡`：最多尝试 300 次寻找满足 ELO 阈值（默认 15%）的方案；
  找不到时再放宽条件寻找任何可行的 5v5 阵容
- `真随机`：随机打乱 + 随机分配，只要 10 人能组成完整阵容就输出结果

#### 为什么不能保证一定成功？

10 个人都要分配 5 个位置之一，且每个队伍每个位置只能有一个人。
如果可用位置不足、或位置锁得太多，可能凑不齐“两套完整五位置”，此时会提示失败，
建议取消部分位置锁定、更换玩家或切换到真随机。

### 二、英雄抽取算法

#### 目标

分组结果确定后，每个玩家从“自己位置的英雄池”里抽英雄；
每人抽 1~3 个（默认 2），所有玩家拿到的英雄全部不重复。

#### 抽取流程

1. 根据当前选择的英雄池，取出每个位置的可选英雄列表
2. 每次尝试前随机打乱 10 位玩家的顺序，避免“固定先选上单、再选打野”导致的先后手偏差
3. 轮到某位玩家时，从他位置池的剩余英雄中随机取“所需数量”个
4. 抽过的英雄用唯一 key 全局标记；即使他同时属于其他位置池，也不会再被抽到
5. 如果某次顺序导致有人凑不满所需数量，就整局重试；最多 400 次
6. 400 次仍无法凑齐“10 × 每人数量”个不重复英雄时，直接报错提示切换更大的英雄池，
   不会偷偷允许重复

#### 几个容易混淆的点

- **一个英雄能打多个位置**：数据里同一英雄会出现在多个位置池（例如剑魔在上单、打野榜都出现）。
  系统按英雄身份去重，而不是按“位置池”去重，所以跨位置也不会撞车
- **同位置两人**：每个位置有两个人，若每人抽 N 个，该位置就需要凑出 2N 个不同英雄，
  再随机分给两人
- **为什么重新打乱**：先被处理的人选择空间更大，每次尝试都重新打乱，
  等于每个位置都有机会“先选”，避免某个位置固定占便宜
- **英雄池大小会影响概率**：池子越小，池内每个英雄被抽中的概率天然越高，
  这是符合直觉的随机结果，不是先后手 bug

## 🛠️ 技术栈

| 类别 | 技术 |
|------|------|
| 框架 | [Next.js 14](https://nextjs.org/)（纯静态导出） |
| 语言 | TypeScript + React 18 |
| 样式 | Tailwind CSS |
| 动画 | Framer Motion |
| 英雄数据 | OP.GG 构建时快照（`data/champions.ts`） |
| 部署 | Cloudflare 静态托管，绑定 `youngzhou6.space` |

## 📁 项目结构

```text
zf-project/
├── app/
│   ├── group/page.tsx        # 分组 + 抽英雄页面（核心功能）
│   ├── page.tsx              # 首页（自动跳转 /group）
│   ├── layout.tsx            # 根布局与站点标题
│   └── globals.css           # 全局样式（动态背景）
├── components/               # 首页展示组件
├── data/
│   ├── players.ts            # 玩家数据 + ELO 档次配置（可手动编辑）
│   └── champions.ts          # OP.GG 英雄榜快照（自动生成，勿手动编辑）
├── lib/
│   └── i18n/                 # 首页国际化
├── public/                   # 静态资源（CNAME 等）
├── scripts/
│   └── update-champions.mjs  # 英雄数据快照更新脚本
├── .github/workflows/        # 备用自动部署工作流
├── package.json
├── next.config.js            # 纯静态导出配置
└── tsconfig.json
```

## 🚀 本地开发

### 环境要求

- Node.js >= 18.0.0
- npm >= 9.0.0

### 安装与启动

```bash
npm install
npm run dev
```

打开 [http://localhost:3000/group](http://localhost:3000/group)。

### 构建与检查

```bash
npm run build   # 构建纯静态站点，产物在 out/
npm run lint    # 代码检查
```

## 🔄 英雄数据如何更新

英雄榜数据以**快照**形式提交在仓库里（`data/champions.ts`），
页面运行时完全不依赖 OP.GG，访问快速稳定。

### 什么时候需要更新

英雄联盟每次版本更新（通常每两周左右）后，OP.GG 榜单会变化。
想使用最新数据时手动更新一次即可。

### 更新命令

```bash
npm run update:champions
```

脚本会依次请求 OP.GG 的上单 / 打野 / 中单 / AD / 辅助五个页面，
解析出每个位置的英雄榜，连同 `Patch 版本 / 服务器 / 分段 / 抓取时间`
一起写回 `data/champions.ts`。

更新完成后重新构建部署：

```bash
npm run build
```

> 页面底部信息会自动变成类似：
> `🎮 英雄数据来自 OP.GG · Patch 16.17 · 单双排 · 全服 · 翡翠+ · 更新 2026-09-04`

> ⚠️ 更新命令需要联网访问 OP.GG。如果 OP.GG 日后改版导致解析失败，
> 脚本会报错并停止，不会覆盖旧快照，页面仍可正常使用旧数据。

### 快照里包含什么

每个位置保存约 40~60 个英雄，每个英雄记录：

- `key`：英雄唯一标识（用于跨位置去重）
- `name`：中文名
- `imageUrl`：头像 CDN 地址
- `tier` / `rank`：OP.GG 评级与排名
- `winRate` / `pickRate` / `banRate`：胜率 / 登场率 / 禁用率

## 👥 玩家与档次

### 档次对照

| 档次 | ELO | 说明 |
|------|-----|------|
| 夯爆了 | 220 | 大师及以上 |
| 夯 | 210 | 钻1钻2 |
| 顶级 | 170 | 钻石 |
| 人上人 | 130 | 翡翠 |
| NPC | 90 | 白金 |
| 拉完了 | 50 | 黄金 |
| 人机 | 40 | 骇人鲸 |

### 页面添加玩家

1. 点击「➕ 添加玩家」
2. 输入玩家名称
3. 为五个位置分别选择档次
4. 点击「确认添加」

### 代码添加玩家

编辑 `data/players.ts`，在 `PLAYERS` 数组中追加：

```typescript
{
  name: '玩家名',
  positions: {
    top: 'npc',
    jungle: 'dingji',
    mid: 'renshangren',
    adc: 'lawanle',
    support: 'npc',
  },
}
```

可用档次：`hangbaole`、`hang`、`dingji`、`renshangren`、`npc`、`lawanle`、`renji`

## 🚀 部署

### Cloudflare 静态托管 + 阿里云域名

项目构建产物是纯静态文件（`out/`），部署到 Cloudflare 静态托管并绑定
阿里云注册的自定义域名 `youngzhou6.space`。

1. **本地构建**

   ```bash
   npm run build
   ```

2. **上传 `out/` 目录**
   - 进入 Cloudflare → Workers & Pages
   - 在对应 Worker/Pages 项目中上传 `out/` 下所有文件

3. **绑定域名**
   - 在 Cloudflare 中添加域名 `youngzhou6.space`
   - Cloudflare 自动配置 DNS 记录

4. **阿里云侧调整 DNS**
   - 到阿里云域名控制台，将 DNS 服务器改为 Cloudflare 分配的地址
   - Cloudflare 接管 DNS 后即可正常访问

仓库根目录还保留了 `vercel.json` 与 `.github/workflows/`，
如需要也可以接入 Vercel / GitHub Pages 作为备用自动部署。

## ❓ 常见问题

**Q: 为什么分组会失败？**

10 人需要组成两套完整五位置阵容，位置不够或锁定太多时可能无解。可以：

- 取消部分位置锁定
- 更换几个玩家
- 切换到「真随机」模式
- 调高 ELO 差值阈值

**Q: 抽英雄会出现重复吗？**

不会。算法全局按英雄唯一标识去重，无论是同一位置两人、还是跨位置都能打同一个英雄，
任何位置都不会撞车；如果实在凑不齐“10 × 每人数量”个不重复英雄，会报错提示，
不会退回“允许重复”的结果。

**Q: 抽英雄报错“凑不出足够多不重复英雄”怎么办？**

说明当前英雄池太小（通常出现在 `Tier 1~2`）。切换到 `全英雄` 或 `热门前 20`
后重新抽即可；也可以把「每人英雄数」调小，或点「🎲 重新抽英雄」多试几次。

**Q: 英雄数据过时了怎么办？**

运行 `npm run update:champions` 更新快照，然后重新构建部署。
页面显示的数据版本就是当前快照的 Patch，一眼能看出新旧。

**Q: 为什么同一个英雄在多个位置都能看到？**

因为该英雄确实能打这些位置（例如剑魔 = 上单/打野）。抽英雄时会全局去重，
这个英雄一旦被某个位置抽走，就不会再分给另一个位置的人。

**Q: 如何修改玩家的档次？**

编辑 `data/players.ts` 中的 `TIER_INFO`（影响 ELO 值、颜色、图标）或
`PLAYERS` 数组中的玩家位置配置。

---

Made with ❤️ using Next.js & Tailwind CSS
