# LOL 内战抽签项目优化建议

## 一、项目理解

### 1. 产品定位

这是一个面向 LOL 开黑/内战场景的静态网页工具，核心流程是：

```text
选择 10 位玩家
  -> 可选地锁定每位玩家的位置
  -> 按 ELO 智能平衡或完全随机分成蓝红两队
  -> 按玩家分到的位置，从英雄池中抽取 1~3 个英雄
  -> 展示队伍、位置、ELO 和英雄结果
```

它不是传统意义上的作品集网站，而是已经演化成了一个“10 人分组 + 英雄抽取”的小工具。当前首页会自动跳转到 `/group`，真正的核心功能都在 [`app/group/page.tsx`](./app/group/page.tsx)。

### 2. 技术形态

| 项目 | 当前实现 |
|---|---|
| 框架 | Next.js 14 App Router |
| 部署模式 | `output: 'export'` 纯静态导出 |
| 前端 | React 18 + TypeScript |
| 样式 | Tailwind CSS + 自定义全局 CSS |
| 动画 | Framer Motion |
| 玩家数据 | [`data/players.ts`](./data/players.ts) |
| 英雄数据 | [`data/champions.ts`](./data/champions.ts) 构建时快照 |
| 数据更新 | [`scripts/update-champions.mjs`](./scripts/update-champions.mjs) |
| 部署痕迹 | Cloudflare、GitHub Pages、Vercel 配置同时存在 |

### 3. 当前代码结构

```text
app/
  page.tsx              # 客户端跳转到 /group
  group/page.tsx        # 分组、位置锁定、抽英雄、弹窗、结果展示
  layout.tsx            # 站点 metadata
  globals.css           # 全局样式和动态背景
components/             # 旧个人作品集组件，当前入口未引用
data/
  players.ts            # 38 位玩家 + 7 档 ELO 配置
  champions.ts          # 约 84 KB 的 OP.GG 英雄快照
lib/i18n/               # 旧作品集的国际化代码，当前入口未引用
scripts/
  update-champions.mjs  # 从 OP.GG 页面抓取并生成快照
```

### 4. 当前验证结果

本地执行 `npm run build` 可以通过，当前构建输出为：

```text
/       499 B    87.8 kB First Load JS
/group  58 kB    145 kB First Load JS
```

构建过程中仍有以下 lint 警告：

- [`app/page.tsx:11`](./app/page.tsx)：`useEffect` 缺少 `router` 依赖。
- [`components/Portfolio.tsx:82`](./components/Portfolio.tsx)：使用了原生 `<img>`。

这说明项目目前的主要问题不是“编译不过”，而是结构、可测试性、状态一致性、数据可靠性和用户体验上的优化空间。

---

## 二、优化优先级总览

| 优先级 | 优化项 | 主要原因 | 建议时机 |
|---|---|---|---|
| P0 | 玩家使用稳定 ID，限制最多选择 10 人 | 当前用姓名作为唯一键，重名会出错；可选出 11 人以上 | 立即 |
| P0 | 统一“修改分组设置后”的状态行为 | 修改英雄设置会自动重抽，但修改分组模式/阈值不会重算 | 立即 |
| P0 | 把英雄抽取从“随机重试”升级为可判定算法 | 当前贪心重试可能误报“无法凑齐” | 立即 |
| P0 | 清理旧作品集代码和首页跳转 | 当前有两套产品身份，且根路径靠客户端跳转 | 立即 |
| P1 | 拆分 1383 行的核心页面 | 算法、状态、UI、弹窗、文案全部耦合 | 近期 |
| P1 | 为分组和抽英雄算法补测试 | 随机算法最容易回归，当前没有自动化测试 | 近期 |
| P1 | 英雄数据增加 schema 校验和稳定来源 | 当前依赖 OP.GG 内部页面结构，改版即失效 | 近期 |
| P1 | 减少首屏 JS、远程字体和图片依赖 | `/group` 首屏 145 kB，数据文件约 84 KB | 近期 |
| P1 | 补齐无障碍和移动端细节 | 弹窗没有焦点管理，部分五列布局在窄屏拥挤 | 近期 |
| P2 | 增加结果分享、持久化和历史记录 | 这是内战工具最直接的体验提升 | 后续 |
| P2 | 优化平衡目标，增加对线级公平 | 当前只优化总 ELO 差，可能出现单路严重错配 | 后续 |

---

## 二点五、匹配算法专项实测结论

这一节专门回答“位置概率、英雄概率、同队概率有没有问题”。结论是：**有，但取决于模式和公平目标**。

| 模式 | 位置概率 | 同队概率 | 英雄概率 |
|---|---|---|---|
| 真随机 | 无位置锁定时每位玩家每个位置约 20%，基本均匀 | 任意两人平均同队概率约 `4/9 = 44.44%` | 仍是随机顺序贪心，不保证所有合法组合等概率 |
| 智能平衡，默认阈值 15% | 接近均匀，但存在小幅偏差 | 明显偏离 44.44%，部分玩家对最多可达 50%~54% | 同上，小英雄池下偏差更明显 |
| 智能平衡，严格阈值 5% | 偏差明显，部分玩家可能被“推向”某些位置 | 偏差更明显 | 同上 |

### 1. 位置概率问题

**算法本身**

- [`randomRolePairing`](./app/group/page.tsx) 单独看是公平的：它把 `[top, top, jungle, jungle, ...]` 这个位置列表随机打乱，因此无位置锁定时，每个玩家每个位置的理论概率都是 20%。
- 但 [`generateBalancedGroups`](./app/group/page.tsx) 会过滤位置分配：只有“蓝红双方能够把总 ELO 分差压到阈值以内”的位置分配才会被接受。
- 因此最终返回的不是“所有合法位置分配等概率”，而是“所有满足 ELO 平衡阈值的位置分配等概率”。一旦阈值很严格，ELO 就会反过来影响位置概率。

**实测**

- 用当前 38 位玩家数据、无位置锁定、每组 10 人、每组 5000 次抽样，随机抽取 50 组阵容：
  - 默认 15% 阈值：每位玩家的最大位置偏差平均约 `1.52` 个百分点，90 分位约 `1.84` 个百分点，最大约 `2.34` 个百分点。
  - 严格 5% 阈值：最大位置偏差平均约 `5.38` 个百分点，90 分位约 `10.1` 个百分点，最大约 `11.72` 个百分点。
  - 5% 阈值下，50 组阵容中有 27 组出现了超过 5 个百分点的位置偏差，6 组超过 10 个百分点。
- 例如某组阵容中，高 ELO 玩家 `牢王Ovo` 在 5% 阈值下打野概率约 `28.95%`，明显高于理论值 20%。

**结论**

- 默认 15% 阈值下，位置概率“接近均匀”，所以 README 中“位置分布均匀”的说法在当前默认设置下大体成立。
- 但不能把它当成数学保证：阈值越严格、位置锁定越多、阵容越特殊，位置概率越容易偏离 20%。
- 位置锁定本身是用户主动约束，不应要求均匀；问题是算法没有区分“用户锁定的确定性”和“ELO 过滤造成的隐性偏向”。

### 2. 同队概率问题

**理论值**

- 真随机模式下，任意两名玩家平均同队概率是 `4/9 ≈ 44.44%`，不是 50%。
- 原因是：同一位置的两人必定分在蓝红两边；只有不同位置的两人才各 50% 同队。随机位置分配下，两人同位置的概率是 `1/9`，所以最终平均同队概率是 `(8/9) × 1/2 = 4/9`。
- 同一位置的两人同队概率恒为 `0`。

**智能平衡模式**

- 智能平衡的目标是最小化总 ELO 差，所以它会主动改变“谁和谁同队”的概率。
- 用第一组 10 人模拟：
  - `忘绝尘 | 牢王Ovo` 在 15% 阈值下同队概率约 `51.9%~52.7%`，明显高于 44.44%。
  - 在 5% 阈值下可达到约 `54.0%`。
- 对 50 组随机阵容统计：
  - 15% 阈值下，最大玩家对同队概率偏离 44.44% 的平均值约 `7.97` 个百分点，90 分位约 `13.0` 个百分点，最大约 `20.4` 个百分点。
  - 50 组中有 38 组出现超过 5 个百分点的玩家对偏差，10 组超过 10 个百分点。

**结论**

- 如果你期望“任意两个人同队概率完全一样”，智能平衡模式一定不满足，这是它的目标决定的，不一定是 bug。
- 但如果你希望结果“看起来公平、没有固定搭子”，当前算法的同队相关性已经强到用户可能感知出来。
- 蓝红方概率在这组模拟中没有明显问题，最大偏离 50% 约 `0.6` 个百分点；主要问题是玩家之间的同队相关性。

### 3. 英雄概率问题

**算法本身**

- [`drawChampionAssignments`](./app/group/page.tsx) 是“随机打乱玩家顺序 + 顺序贪心选择 + 失败重试”。
- 随机顺序解决了“固定某个位置先选”的问题，但它不能让所有合法英雄分配等概率。
- 一个英雄如果同时出现在多个位置池中，会被更早选走，从而改变后续玩家的可选空间；不同英雄的边际概率因此不同。

**实测**

- 对两个玩家、每人抽 2 个英雄的合法分配做精确枚举：
  - `全英雄` 的 `mid | adc` 组合：最可能分配约是最不可能分配的 `1.089` 倍；相对“所有合法分配等概率”的基准，最高约高 `5.8%`，最低约低 `2.8%`。
  - `热门前 20` 的 `mid | adc` 组合：最可能分配约是最不可能分配的 `1.242` 倍；最高约高 `18.0%`，最低约低 `4.97%`。
  - `Tier 1~2` 的 `top | mid` 组合：最可能分配约是最不可能分配的 `1.209` 倍；最高约高 `17.5%`，最低约低 `2.8%`。
- 当前数据中，10 人、每人 1~3 个英雄、三种英雄池各模拟 5000 次，没有出现“凑不齐”的失败。
- 但这只是经验结果，不是算法保证。英雄池变小、OP.GG 数据变少、未来增加更多位置限制时，400 次随机重试可能误报“无法凑齐”。

**结论**

- “同一个位置池内，随机洗牌后取若干英雄”在局部是均匀的。
- “全场不重复”加入后，最终每个英雄、每种合法分配的概率不再严格均匀。
- 偏差在 `全英雄 + 每人 1~2 个` 时较小；在 `热门前 20`、`Tier 1~2`、每人 2~3 个时更明显。

### 4. 为什么会同时出现这些偏差

当前算法把四个目标混在了一起：

1. 位置尽量随机；
2. 队伍总 ELO 尽量平衡；
3. 同队/对手关系尽量公平；
4. 英雄尽量不重复且概率均匀。

这四个目标不可能同时完全满足。当前实现实际上是：

```text
位置随机分配
  -> 按 ELO 过滤出“能平衡”的位置分配
  -> 在能平衡的分配中随机选一套
  -> 对每个位置做蓝红最优切分
  -> 英雄按随机顺序贪心抽取
```

所以：

- 位置概率的偏差来自“按 ELO 过滤”。
- 同队概率的偏差来自“最小化总 ELO 差”。
- 英雄概率的偏差来自“顺序贪心 + 全局去重”。

### 5. 建议的改法

**如果最看重位置随机**

- 不要用 ELO 阈值过滤位置分配；
- 先均匀随机决定位置，再在队伍切分阶段做平衡；
- 如果平衡不达标，要么接受结果，要么重新抽位置，但不要把“平衡不达标”作为位置分配的隐性筛选条件。

**如果最看重同队公平**

- 不要只最小化总 ELO 差；
- 把“同队概率”和“对线差”加入目标函数，例如：

```text
score =
  w1 * |总 ELO 差|
  + w2 * Σ|每条路 ELO 差|
  + w3 * max(每条路 ELO 差)
  + w4 * 同队历史次数
```

- 或者明确把“平衡模式”和“完全随机模式”的公平目标写清楚：平衡模式本来就不是等概率同队。

**如果最看重英雄分配公平**

- 用二分图匹配或最大流先保证存在合法解；
- 再在合法解中随机选一个，而不是顺序贪心后重试；
- 如果无法做到“所有合法分配等概率”，至少做到：
  - 每个玩家在自己池内的边际概率尽量接近均匀；
  - 不因为玩家顺序产生系统性偏差；
  - 保证存在解时不会误报失败。

**工程上必须补的**

- 引入可复现的 seeded RNG；
- 为位置分布、同队概率、英雄边际概率写统计测试；
- 给结果加 seed 或分享参数，方便复盘“这次是不是算法偏了”。

---

## 三、P0：优先处理的问题

### P0-1. 玩家改用稳定 ID，并限制最多选择 10 人

**现状**

- `selected` 存的是玩家姓名，`lockedPositions` 也用姓名作为 key。
- `allPlayers` 由基础玩家和临时玩家拼接，没有检查重名。
- 页面没有阻止用户选第 11 个人；按钮虽然会禁用，但文案会显示“还需 -1 人”。

相关位置：

- [`app/group/page.tsx:373`](./app/group/page.tsx)：`customPlayers`、`selected`、`lockedPositions` 状态。
- [`app/group/page.tsx:396`](./app/group/page.tsx)：按姓名切换玩家。
- [`app/group/page.tsx:421`](./app/group/page.tsx)：只在 `selected.length !== 10` 时禁止生成。
- [`app/group/page.tsx:848`](./app/group/page.tsx)：按钮的禁用条件和剩余人数文案。

**风险**

- 两个玩家重名时，React key 冲突，选择/删除/锁定可能互相影响。
- 选择超过 10 人后，状态仍然是合法的，只是按钮不可用，用户不知道下一步该做什么。
- 以后增加“玩家账号”“玩家备注”“头像”时，姓名作为主键无法扩展。

**建议**

1. 给 `Player` 增加不可变的 `id`：

```ts
interface Player {
  id: string;
  name: string;
  positions: Record<Position, TierKey>;
}
```

2. 所有 `selected`、`lockedPositions`、`championDraws` 都使用 `playerId` 作为 key。
3. 添加玩家时校验：
   - 名称不能为空；
   - 名称不能与已有玩家重复；
   - 可以自动生成 `crypto.randomUUID()` 作为 ID。
4. 选择第 11 人时直接阻止，并提示“最多选择 10 人”，不要让界面进入 `10 - selected.length` 为负数的状态。
5. 为“清空”增加二次确认，避免误触丢失当前分组。

### P0-2. 统一“修改设置后是否重新分组”的行为

**现状**

页面提示“已出结果时修改设置会立即按新配置重新抽取”，但实际行为不一致：

- 修改英雄池或每人英雄数，会立即重新抽英雄。
- 修改分组模式或 ELO 阈值，不会重新分组。
- 修改 ELO 阈值后，旧结果仍然显示，但结果颜色会按新阈值重新判定。

相关位置：

- [`app/group/page.tsx:477`](./app/group/page.tsx)：切换英雄池时立即重抽。
- [`app/group/page.tsx:492`](./app/group/page.tsx)：修改英雄数量时立即重抽。
- [`app/group/page.tsx:730`](./app/group/page.tsx)：切换分组模式只修改 `sortMode`。
- [`app/group/page.tsx:772`](./app/group/page.tsx)：修改阈值只修改 `threshold`。

**建议**

二选一，并保持全局一致：

方案 A：所有影响结果的设置都自动重算。

```text
修改分组模式 -> 重新分组 + 重新抽英雄
修改 ELO 阈值 -> 重新分组 + 重新抽英雄
修改英雄池 -> 只重抽英雄
修改每人英雄数 -> 只重抽英雄
```

方案 B：设置变更后把结果标记为“配置已变更”，显示“重新分组”按钮，由用户主动触发。

对于内战工具，推荐方案 B。因为自动重算会让用户在调整设置时频繁看到结果跳动，也可能覆盖刚刚看好的阵容。

### P0-3. 英雄抽取升级为“先判可行性，再保证可解”

**现状**

`drawChampionAssignments` 的做法是：

1. 随机打乱 10 位玩家；
2. 每位玩家从自己位置池里随机选若干英雄；
3. 全局记录已抽英雄；
4. 如果某次顺序失败，整局重试，最多 400 次；
5. 400 次失败后直接提示用户换更大的英雄池。

相关位置：

- [`app/group/page.tsx:293`](./app/group/page.tsx)：英雄池筛选。
- [`app/group/page.tsx:300`](./app/group/page.tsx)：随机重试式抽取。

**问题**

这是典型的贪心算法。即使存在一种合法分配，随机顺序也可能连续失败，最后被误判为“无法凑齐”。英雄池越小、跨位置英雄越多，误判概率越高。

**建议**

把“抽英雄”建模为匹配问题：

- 左侧是 10 位玩家，每位玩家需要 `heroCountPerPlayer` 个英雄；
- 右侧是当前英雄池中的英雄；
- 每位玩家只能连到“自己位置池”中的英雄；
- 每个英雄最多被选择一次；
- 用二分图匹配或最大流求解，能够直接判断“是否存在解”，并返回一个合法分配。

如果不想引入图算法依赖，至少做以下改进：

1. 预先计算每个位置池、以及所有位置池的并集大小。
2. 在点击“开始分组”前，先检查理论上是否可能，给出明确提示，而不是等 400 次失败。
3. 按候选数量从少到多排序玩家，优先处理选择空间小的人，降低贪心失败率。
4. 保留回溯搜索作为小规模场景的兜底。
5. 把 400 次重试改成“确定性搜索 + 随机打散结果”，保证不会因为随机顺序误报失败。

### P0-4. 清理旧作品集代码，明确项目只保留 LOL 工具身份

**现状**

当前 `app/page.tsx` 只负责跳转，但仓库中仍保留：

- `components/Hero.tsx`
- `components/About.tsx`
- `components/Portfolio.tsx`
- `components/Navigation.tsx`
- `components/Footer.tsx`
- `components/i18n/LanguageSwitcher.tsx`
- `lib/i18n/LanguageContext.tsx`
- `lib/i18n/translations.ts`
- `README-old.md`

这些文件当前入口没有引用，但会带来维护成本：

- 增加代码搜索噪音；
- 让新人误以为项目是作品集网站；
- 保留旧 i18n、旧远程图片和旧设计 token；
- 构建时仍可能触发旧代码的 lint 警告。

**建议**

1. 如果不再需要作品集页面，删除或移动到独立的 `archive/portfolio` 仓库。
2. 删除 `README-old.md`，只保留当前 LOL 工具文档。
3. 清理 Tailwind 中只为旧页面服务的颜色、字体和动画 token。
4. 删除 `package.json` 中未使用的直接依赖 `fill-range`、`streamsearch`。
5. 清理 `next.config.js` 中没有实际作用的 `images.remotePatterns` 通配配置。

### P0-5. 首页跳转改为真正的入口，避免客户端闪屏

**现状**

[`app/page.tsx`](./app/page.tsx) 是一个客户端组件：

- 使用 `useEffect` 调用 `router.replace('/group')`；
- JSX 里又放了一个 `<meta http-equiv="refresh">`；
- 这个 meta 标签位于页面内容中，不是标准的 `<head>` 位置。

这会导致：

- 首次加载先渲染“跳转中”，再执行客户端跳转；
- 多加载一段客户端 JS；
- 搜索引擎看到的是一个几乎空白的页面；
- 静态托管环境下无法使用标准 301/302 重定向。

**建议**

优先方案：把 `/group` 的核心页面直接作为根路由 `/`，这样用户访问首页就是工具本身。

如果必须保留 `/group` 路径，可以选择：

1. 在 Cloudflare Pages 配置 `_redirects`，让 `/` 重定向到 `/group`；
2. 或在 `app/page.tsx` 中复用同一个分组组件，而不是做客户端跳转；
3. 至少把 `meta` 放到正确的 head 元数据中，并补上 `router` 依赖。

---

## 四、P1：结构和工程质量优化

### P1-1. 拆分 1383 行的核心页面

[`app/group/page.tsx`](./app/group/page.tsx) 目前约 1383 行、60 KB，承担了：

- 类型定义；
- 常量配置；
- 洗牌和分组算法；
- 英雄池筛选；
- 英雄抽取；
- 13 个 `useState`；
- 页面状态逻辑；
- 玩家选择 UI；
- 位置锁定 UI；
- 设置面板；
- 结果展示；
- 3 个弹窗；
- 算法说明文案；
- 档次表。

建议拆分方向：

```text
lib/grouping/
  types.ts             # Position、TierKey、Player、GroupedPlayer、Team
  constants.ts         # POSITIONS、TEAM_NAMES、TIER_INFO
  random.ts            # shuffle、可选 seeded RNG
  assign-roles.ts      # randomRolePairing、backtrackRolePairing
  split-teams.ts       # balancedSplitFromRoles、randomSplitFromRoles
  generate-groups.ts   # generateBalancedGroups、generateRandomGroups

lib/champions/
  types.ts             # ChampionEntry、ChampionPoolMode
  pool.ts              # championPoolFor
  draw.ts              # drawChampionAssignments / matching 算法

components/group/
  PlayerSelector.tsx
  GroupSettings.tsx
  PositionLockEditor.tsx
  TeamResult.tsx
  ChampionPickCard.tsx
  ChampionPoolModal.tsx
  AlgorithmModal.tsx
  AnnouncementModal.tsx
  TierTable.tsx
```

拆分后有三个直接收益：

1. 算法可以单独写单元测试，不需要渲染整个页面。
2. 弹窗和结果组件可以按需加载，减少 `/group` 首屏 JS。
3. 修改 UI 时不容易误伤分组逻辑。

### P1-2. 为算法建立自动化测试

当前仓库没有测试脚本。对于随机算法，这是最大的工程风险。

建议至少覆盖：

**分组测试**

- 10 人一定分成两队、每队 5 人；
- 每个队伍五个位置各一人；
- 位置锁定的玩家只出现在允许位置；
- 没有重复玩家；
- 智能平衡模式下，能找到解时结果分差不超过阈值；
- 无解时返回明确的失败状态，而不是空队伍被当作成功。

**英雄抽取测试**

- 每位玩家拿到的英雄数量正确；
- 每位玩家的英雄都来自其位置池；
- 全场英雄不重复；
- 小英雄池场景不会因为随机顺序误报失败；
- 英雄池切换后重新抽取结果符合新池规则。

**建议引入**

- `vitest`：轻量、适合 Vite/Next 项目；
- `fast-check`：可选，用于生成大量随机玩家配置做属性测试；
- `seedrandom` 或自研小型 PRNG：让测试和分享结果可复现。

### P1-3. 增加 CI，让每次提交自动检查

目前只有部署工作流，没有独立的“质量门禁”。建议新增：

```yaml
# .github/workflows/ci.yml
- npm ci
- npm run lint
- npm run typecheck
- npm test
- npm run build
```

同时给 `package.json` 增加：

```json
{
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  }
}
```

这样可以把“能部署”升级为“每次提交都能验证算法和类型”。

### P1-4. 英雄数据增加 schema 校验和稳定来源

**现状**

[`scripts/update-champions.mjs`](./scripts/update-champions.mjs) 通过解析 OP.GG 页面中的 `self.__next_f.push` 数据来生成快照：

- [`scripts/update-champions.mjs:48`](./scripts/update-champions.mjs)：解析 Next.js 内部数据块。
- [`scripts/update-champions.mjs:75`](./scripts/update-champions.mjs)：按 `positionTierData` 识别榜单。
- [`scripts/update-champions.mjs:228`](./scripts/update-champions.mjs)：直接写回 `data/champions.ts`。

这依赖 OP.GG 的内部页面结构。OP.GG 一旦改版，脚本可能失败，甚至解析出结构相似但不完整的数据。

**建议**

1. 优先使用 OP.GG 的公开接口或稳定数据源；如果没有公开接口，至少把页面解析隔离成独立适配器。
2. 在写入前增加 schema 校验：
   - 每个位置至少 N 个英雄；
   - `key`、`name`、`imageUrl` 必须存在；
   - `tier`、`rank`、`winRate`、`pickRate` 必须是数字；
   - 排名不能重复，胜率范围合理。
3. 写入采用“先写临时文件，再原子替换”，避免中途失败留下半截数据。
4. 增加数据更新时间、来源和 patch 的展示与告警。
5. 可选：增加定时 GitHub Action，自动更新快照并创建 PR，而不是每次手动运行。
6. 确认 OP.GG 的使用条款、频率限制和署名要求；数据抓取失败时保持旧快照可用。

### P1-5. 优化首屏性能和外部资源

**现状**

- `data/champions.ts` 约 84 KB，作为客户端组件的 import 被打进 `/group` 页面。
- `/group` 首屏 First Load JS 为 145 kB。
- [`app/globals.css:1`](./app/globals.css) 通过 Google Fonts 远程加载 Inter 和 Playfair Display。
- 英雄头像来自 `opgg-static.akamaized.net`，字体来自 Google，都是运行时外部依赖。
- [`next.config.js`](./next.config.js) 使用 `output: 'export'`，图片被设置为 `unoptimized: true`。

**建议**

1. 把英雄数据改成更紧凑的格式，例如数组元组或按位置拆分，减少重复字段。
2. 如果图片 URL 可以根据 `patch + key` 推导，就不必在每条数据里存完整 URL。
3. 弹窗内容、算法说明和英雄池预览按需加载，避免首屏一次性加载所有弹窗代码。
4. 字体改为 `next/font/local` 自托管，或至少减少字重和字体族；中文用户访问 Google Fonts 可能不稳定。
5. 给英雄头像增加本地兜底、错误占位和更明确的尺寸属性，避免 CDN 失败时出现空白。
6. 保留静态导出时，不要期待 `next/image` 的服务端优化能力；重点是减少数据体积、延迟加载和 CDN 缓存。

### P1-6. 补齐无障碍和移动端体验

**现状**

- 三个弹窗都只通过点击遮罩关闭，没有 `role="dialog"`、`aria-modal`、焦点锁定和 Escape 键关闭。
- 弹窗打开时页面背景仍可能滚动。
- 动画没有适配 `prefers-reduced-motion`。
- 添加玩家表单使用 `grid-cols-5`，窄屏下五个下拉框会非常拥挤。
- 部分纯图标按钮缺少 `aria-label`。

**建议**

1. 弹窗统一使用可访问的 Dialog 组件，支持：
   - 打开时聚焦到弹窗；
   - 焦点限制在弹窗内；
   - Escape 关闭；
   - 关闭后焦点回到触发按钮；
   - 打开时锁定 `body` 滚动。
2. 在 `globals.css` 中加入 `prefers-reduced-motion`，关闭或弱化光晕、漂浮和位移动画。
3. 把添加玩家和位置锁定中的五列布局改成响应式：

```text
手机：grid-cols-1 或 grid-cols-2
平板：grid-cols-3
桌面：grid-cols-5
```

4. 为关闭、删除、锁定等纯图标按钮补 `aria-label`。
5. 用键盘走查一次完整流程：选人 -> 锁位置 -> 分组 -> 查看结果 -> 重抽英雄。

---

## 五、P2：产品和算法增强

### P2-1. 增加结果分享和导出

内战工具最实用的下一步不是继续堆动画，而是让结果容易发给队友。

建议支持：

- 复制纯文本结果到微信/QQ：

```text
蓝方（总 ELO 720）
上单：玩家A / 英雄1 / 英雄2
打野：玩家B / 英雄1 / 英雄2
...
```

- 复制分享链接；
- 生成结果图片；
- 把分组结果写入 URL 参数，方便队友打开同一套阵容。

如果要让结果可复现，建议同时引入 seed：

```text
/group?players=...&mode=balanced&seed=abc123
```

这样同一 seed 可以还原同一套分组和英雄抽取结果，适合复盘和“谁改结果”的场景。

### P2-2. 增加本地持久化和历史记录

当前刷新页面后，已选玩家、位置锁定和分组结果都会丢失。

建议：

1. 用 `localStorage` 保存最近一次选择、位置锁定和设置；
2. 增加“最近分组”列表；
3. 版本公告用 `localStorage` 记录已读版本，避免每次打开都弹窗；
4. 对自定义玩家提供“保存到本地”而不是只存在于当前页面会话。

### P2-3. 把平衡目标从“总 ELO 差”升级为“对线级公平”

当前 `balancedSplitFromRoles` 的目标是让两队总 ELO 尽量接近。这个目标有一个局限：

```text
蓝方总 ELO = 红方总 ELO
```

不代表每条路的对线都公平。例如：

```text
蓝方上单 220 vs 红方上单 40
蓝方打野 40  vs 红方打野 220
蓝方中单 90  vs 红方中单 90
...
```

总分可能接近，但上单和打野的体验会非常差。

建议把目标函数改为组合评分，例如：

```text
score =
  w1 * |总 ELO 差|
  + w2 * Σ|每条路 ELO 差|
  + w3 * max(每条路 ELO 差)
```

再配合随机扰动，在多个高分方案中随机选择。这样既保持整体平衡，又能减少单路碾压。

### P2-4. 增加玩家偏好和分组约束

可以按需求逐步增加：

- 每个位置设置“偏好/可玩/不可玩”，而不是只有锁定；
- ✅ 支持“这两个人不要同队”；
- ✅ 支持“这两个人必须同队”；
- 支持队长固定；
- 支持上局同队的人尽量拆开；
- 支持不同队伍人数，例如 4v4、3v3。

这些功能需要先把算法从页面中抽离出来，否则会让 `page.tsx` 继续膨胀。

### P2-5. 玩家选择体验优化

当前 38 位玩家全部平铺展示，后续人数增加后会很难找。

建议：

- 增加搜索框；
- 增加“已选/未选”分组；
- 显示当前已选顺序；
- 随机选 10 人后显示本次随机结果；
- 支持“最近常用玩家”；
- 自定义玩家重名时给出明确错误。

### P2-6. 注意玩家数据隐私

`data/players.ts` 中包含真实玩家昵称，并随静态站点公开。如果这些是真实社交关系中的昵称，建议确认是否适合公开；也可以改成匿名编号或只在本地保存。

---

## 六、建议的实施顺序

### 第一阶段：0.5~1 天，先修高风险体验

1. 玩家改用 `id`，禁止重名，限制最多 10 人。
2. 统一“修改设置后是否重算”的行为。
3. 清理旧作品集组件、旧 i18n、`README-old.md` 和未使用依赖。
4. 首页直接承载分组工具，或在托管层做真实重定向。
5. 给英雄抽取增加前置可行性检查和更清晰的错误提示。

### 第二阶段：1~2 天，建立可持续结构

1. 把分组算法、英雄算法、类型和常量从页面中拆出。
2. 拆分玩家选择、设置、结果、弹窗等组件。
3. 引入 Vitest，为算法补核心测试。
4. 增加 CI 的 lint、typecheck、test、build。
5. 给数据更新脚本增加 schema 校验和原子写入。

### 第三阶段：按产品需要迭代

1. 分享文本/图片/链接；
2. localStorage 持久化和历史记录；
3. 对线级公平评分；
4. 玩家偏好、避免同队、队长固定；
5. 更稳定的英雄数据源和图片兜底。

---

## 七、验收清单

完成优化后，建议至少满足：

- [ ] `npm run build` 通过；
- [ ] `npm run lint` 无警告；
- [ ] `npm run typecheck` 通过；
- [ ] 分组算法有单元测试；
- [ ] 英雄抽取有单元测试；
- [ ] 最多只能选择 10 位玩家；
- [ ] 重名玩家不会导致选择或锁定错乱；
- [ ] 修改分组设置后，结果状态与界面提示一致；
- [ ] 位置锁定在分组结果中 100% 生效；
- [ ] 英雄不重复规则在所有英雄池下可验证；
- [ ] 手机端可以完成完整操作；
- [ ] 键盘可以关闭弹窗并完成主要流程；
- [ ] 英雄图片 CDN 失败时有兜底；
- [ ] 首页没有客户端跳转闪屏；
- [ ] 仓库中不再保留当前入口未使用的旧产品代码。

---

## 八、总结

这个项目已经具备一个可用内战工具的核心闭环：玩家选择、位置锁定、ELO 平衡、英雄抽取和结果展示。当前最值得做的不是继续增加动画，而是先把三件事做扎实：

1. **把身份和状态管理做正确**：稳定 ID、10 人上限、设置变更一致性。
2. **把随机算法变成可验证算法**：分组和抽英雄都要有测试，并且避免因为随机重试误报失败。
3. **把核心页面拆开**：算法、数据、组件分离后，后续增加分享、历史记录和对线级平衡才不会继续堆在一个文件里。

完成这些基础优化后，再增加“分享结果”“保存历史”“按对线平衡”这类产品功能，收益会更高，也更容易维护。
