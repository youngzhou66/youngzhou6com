# 版本公告

## v1.7 — 2026-09-16

### 新增玩家搜索与标签筛选

- 选人页新增**搜索框**，按玩家 id 实时过滤
  - 大小写不敏感的子串匹配（`meiko`、`MEIKO`、`mei` 都能搜到「成都meiko」）
  - 支持**空格分词**：`状态 一换一` 能定位到「状态好能一换一」；多个关键词需要全部命中
- 新增**标签筛选栏**：列出当前名单里实际出现过的标签，点击即筛，可多选
  - 多选标签取**交集**（AND）：只显示同时具备全部所选标签的玩家
  - 标签与搜索词之间也是交集，可以组合使用（如「咕咕嘎嘎」+ 搜「大灰蛛」）
- 显示「共 N 人，匹配 M 人」的实时计数，可一键**重置筛选**
- 无匹配结果时显示空状态提示，并提供重置入口
- 搜索只影响**展示**，不改变已选中的玩家；筛选状态下随时可以点卡片继续增减
- 标签栏排序：预设标签在前，自定义标签按出现次数降序（同次数按名称），顺序稳定好找

### 为什么关键字和标签都取「交集」

搜索的直觉是**越搜越窄**：多打一个词、多点一个标签，结果应该更少才对。
如果取并集，多选标签反而会冒出更多玩家，和搜索的心智模型相反。

## v1.6 — 2026-09-15

### 新增玩家标签

- **常驻成员的标签直接写进 `data/players.ts`**（进 git，所有人打开页面就能看到），
  临时玩家的标签在「添加玩家」时确定、存浏览器本地
- 39 位常驻成员中 26 位已打标签：`咕咕嘎嘎` 9 人、`再次抽象` 7 人、`精英咕咕嘎嘎` 6 人，
  另有 4 个专属标签（`牢王` / `大董` / `羊羊` / `白虎`）
- 标签用于识别与娱乐；**不参与分组计算**，不影响 ELO 与平衡结果
- 支持预设标签与自定义标签：
  - 预设：`咕咕嘎嘎` / `精英咕咕嘎嘎` / `ikun翡翠兄弟` / `ikun战神` / `再次抽象`
  - 自定义标签：自动去空白与控制字符、去重、截断到 8 字，每人最多 5 个
- **所有标签统一使用青色调**（取自 `咕咕嘎嘎`）：预设与自定义一视同仁，不做一标签一色。
  颜色多了反而花，且自定义标签无法保证两两不同色。想换主题色只改
  `lib/players/tags.ts` 里的 `TAG_COLOR`
- 选人页玩家卡片与结果页蓝/红队伍卡片都会显示标签 chip
- 选人页玩家卡片改为**统一尺寸**（132×72），名字长短、有无标签都不影响卡片大小
  - 名字超长自动省略号截断 + 悬停显示全名
  - 多标签只显示第一个 chip，多余的用 `+N` 表示，悬停可看全部
- **玩家名长度上限 8 字**（名单里最长的名字是 7 字，所以不会截断任何现有玩家），
  添加玩家时输入框直接限长并给出明确报错
- **页面不提供标签编辑入口**：常驻成员改标签 = 改 `data/players.ts` 重新部署

### 新增临时玩家本地缓存

- 临时玩家改为**持久化在访问者自己的浏览器**（localStorage），刷新、关页再开都不会丢
- 选人页新增独立管理区块：显示人数与“仅保存在本机”提示，支持**逐个删除**与**一键全部删除**
  （一键删除需二次确认）
- 删除临时玩家时会连带清理该玩家的选中状态、位置锁定、同队约束、英雄抽取结果与标签，
  不再残留孤儿数据
- localStorage 不可用（隐私模式 / 配额满）时静默降级为“本次会话内有效”，不会报错

### 修复：玩家重名会导致静默错乱

- 玩家 `name` 在整条链路里充当主键（选中 / 位置锁定 / 同队约束 / 英雄抽取结果），
  此前没有重名校验，添加一个与已有玩家同名的临时玩家会出现“点一个选中两个”、
  位置锁定串味、英雄结果互相覆盖等问题
- 现在新增玩家时会校验重名并给出明确提示（`“羊羊” 已存在，请换个名字`），
  从缓存加载时也会丢弃重名、缺字段、非法档位的坏条目

### 工程

- 新增 `lib/players/tags.ts`（标签预设与清洗规则）、`lib/players/search.ts`（搜索与筛选谓词）、
  `lib/players/storage.ts`（本地持久化 + schema 校验 + 重名守卫）、
  `components/group/TagPicker.tsx`（添加玩家时的标签选择器）
- 新增 `tests/players/` 下三组测试：标签规则、搜索筛选、本地缓存，
  覆盖清洗去重上限、大小写与分词匹配、多标签交集、标签随玩家持久化、坏缓存兜底与重名守卫

## v1.5.1 — 2026-09-15

### 文案诚实化：说清“总分差”到底约束了什么

一次 200 组 × 每组 300 次配对的实测，暴露了原说明与算法实际行为不符：

- **无位置锁定时，1%~60% 所有阈值达成率都是 100%**：5 个位置各 2 人、共 32 种蓝红切分，
  让“总分差”成为一个很容易精确凑到 0 的子集和问题，算法几乎总能找到总分差 ≈ 0 的完美五五开，
  因此默认 10% 阈值实际很少成为约束条件，滑杆平时拉大拉小结果一样
- 阈值真正生效的场景是**位置锁定或同队约束**把组合空间压小、凑不出完美平衡时：
  实测 3 人各锁最强位时，1% 阈值达成率降到 60%、3% 降到 75%、5% 降到 94%
- **总分差为 0 不代表每条路都公平**：实测单路最大分差可达约 158 分（≈ 5 个档位），
  只是被另一路的反向差距抵消掉了——算法只保证总分接近，不保证对线体验

本次只改文案与说明，**算法与参数一行未动**：

- 「加权 ELO 差值阈值」滑杆下补充说明：阈值只管队伍总分差、平时为何无效、何时才生效
- 结果卡片补充说明：总分差 0 ≠ 每条路都公平
- 「算法原理」弹窗新增「实测结论」小节，写明上述数据
- 平衡目标从“总分接近”升级为“对线级公平”留待后续版本（需改目标函数）

### 修复：胜率显示方向错误

- **症状**：有时蓝色方 ELO 总分更低，胜率却显示得更高
- **原因**：计算胜率时误用了分差的**绝对值**，而 `P = 1/(1+10^(-Δ/400))` 在 `Δ ≥ 0` 时恒 ≥ 50%，
  于是“蓝方胜率”永远不低于 50%，等于把胜算固定判给蓝方
- **修复**：改用带符号差值 `蓝方总分 − 红方总分` 计算蓝方胜率，红方取其补数；
  绝对值只用于展示“分差”。现在 ELO 更高的一方胜率必然更高，两者相加恒为 100%
- 顺带澄清：算法本身**没有偏袒蓝方**。20000 次分组实测，蓝方总分更高占 46.66%、
  红方更高占 47.17%、平均总分差仅 `-0.051`，32 种切分被均匀选中（平均置位数 2.500），
  蓝方偏高只是上面的显示 bug 造成的观感
- `winProbabilityFor` 从组件提取到 `lib/grouping/elo.ts` 并补充单元测试，
  覆盖方向性、互补性（相加为 1）、单调性和 Δ400 ≈ 10 倍胜算

## v1.5 — 2026-09-15

### ELO 刻度重标定

- 档位 ELO 从“局部 10 分、整体 180 分”改为**相邻档位统一相差 30 分**
- 旧刻度下 `夯爆了(220) vs 夯(210)`、`拉完了(50) vs 人机(40)` 只差 10 分，对应单人胜率 51.4%，
  等于抛硬币，却占掉了两个独立档位
- 新刻度（220 / 190 / 160 / 130 / 100 / 70 / 40）每档约对应 54% 单人胜率，
  是“能感觉到、但不过分”的粒度
- 位置权重（上单/AD ×0.95、中单/打野 ×1.05、辅助 ×1.00）与默认阈值 10% 保持不变；
  五个权重之和恰为 1.00，队伍加权总分刻度不变（实测均值约 576），阈值语义不受影响
- 人机档标注补充为「黑铁 / 骇人鲸」，档位说明不再只是个梗
- 结果卡片新增**估算胜率**（`P = 1 / (1 + 10^(-Δ/400))`），比单纯的百分比分差更直观
- 修复结果卡片颜色判定：过去最严格一档写死在 10%，用户把阈值调到 5% 时，
  8% 的分差仍会显示成黄色“差距不大”

## v1.4 — 2026-09-10

### ELO 位置权重

- 分组平衡改为按位置加权后的 ELO 计算
- 上单 / AD 权重为 0.95，中单 / 打野权重为 1.05，辅助权重为 1.00
- 智能平衡模式使用加权 ELO 计算蓝红分差，让阵容强度评估更贴近实际对局价值
- 默认加权 ELO 差值阈值从 15% 调整为 10%
- 队伍卡片同时展示原始 ELO 与加权 ELO，方便核对计算过程
- 真随机模式仍只保证阵容结构合法，不参与 ELO 平衡

## v1.3 — 2026-09-09

### 新增同队约束

- 支持为任意两名玩家设置“必须同队”或“不要同队”
- 智能平衡与真随机两种模式均严格遵守同队约束
- 同一位置的两人无法“必须同队”；当约束互相冲突或与位置锁定一起导致无解时，页面会明确提示分组失败

### 工程与体验整理

- 根路径直接进入分组工具，移除旧作品集页面和客户端跳转
- 拆分核心页面与算法模块，分组、约束和英雄抽取逻辑独立维护
- 新增 Vitest 自动化测试，覆盖 5v5 结构、位置锁定、同队约束和英雄抽取规则

## v1.2 — 2026-09-08

### 重构分组算法

- 修复旧算法“为了平衡 ELO 总把多位置玩家推到弱位置”的问题
  （例如羊羊旧算法下约 70% 概率被分配到 AD）
- 新逻辑：
  1. 先随机把 10 人分配到 10 个位置名额上（每个位置 2 人，蓝/红各 1 人）
  2. ELO 只用于决定每个位置的两名玩家谁去蓝方、谁去红方
  3. 从分差 ≤ 15% 的多套阵容中随机返回一套
- 位置锁定逻辑保留；真随机模式继续可用
- 已验证：30 人每人抽查 300 次，位置分布均匀，无固定偏向

### 更新英雄数据

- 重新从 OP.GG 抓取上单 / 打野 / 中单 / AD / 辅助五个位置的英雄榜
- 同步更新英雄 Tier 评级、排名、胜率 / 登场率 / 禁用率
- 数据快照时间：2026-09-08

## v1.1 — 2026-09-04

### 新增分组后直接抽取英雄

- 分组完成后，按每个人分到的位置自动抽取英雄
- 英雄榜以构建时快照形式保存，页面不依赖 OP.GG 实时接口
- 同一局内所有玩家拿到的英雄不重复
- 支持英雄池预览、重新抽英雄（只重抽英雄，不改变队伍）

---

Made with ❤️ for LOL 开黑
